'use client'

// CopilotKit integration disabled - requires agent setup
// To re-enable: wrap app in CopilotProvider and configure agents

export function JobAssistant() {
  // Component disabled until CopilotKit agents are configured
  return null
}
